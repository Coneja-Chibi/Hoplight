node_modules/
dist/
build/*
!build/vaude.ico
!build/vaude-256.png
*.log
.DS_Store
.env
.env.*
bun.lockb

# Large binary samples: kept locally / downloaded on demand, referenced by URL in each SOURCES.md.
# The small sliced card.json fixture beside them IS committed for tests.
samples/**/*.charx
design/_serve.cjs
*.bun-build

# Agent-tooling state: never ships
.agents/
.claude/
CLAUDE.md
audit-out/

# local design wireframes (never shipped)
wireframes/

# internal planning notes (the vaud-notes; never shipped, like the old design/ folder)
vaud-notes/
external-refs/
