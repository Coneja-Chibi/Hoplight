/** Persona entity barrel (folders-as-schema; the regex/lorebook siblings' shape). */
export type {
  CanonicalPersona,
  PersonaAttribution,
  PersonaBody,
  PersonaChatInjection,
  PersonaIdentity,
  PersonaPresentation,
  PersonaPronounSet,
  PersonaSections,
} from "./schema";
export { emptyPersonaBody, SECTION_ORDER_DEFAULT } from "./schema";
