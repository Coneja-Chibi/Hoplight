/**
 * Vendor stub: the shared automatic-JSX runtime, bundled against the SAME React instance
 * (react stays external here and resolves through the import map). See vendor/react.ts.
 */
export * from "react/jsx-runtime";
