/**
 * Vendor stub: the shared DOM renderer entry, bundled against the SAME React instance
 * (react and the jsx runtime stay external here and resolve through the import map).
 */
export * from "react-dom/client";
